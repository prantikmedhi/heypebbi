# Compatibility entry point

The canonical repository instructions are [AGENTS.md](AGENTS.md). Read that file in full before acting. This singular filename exists because it was explicitly requested; it must not acquire a second, conflicting instruction set.
